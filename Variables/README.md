# Variables

Crimes against naming, scope, and the idea that a variable should mean one consistent thing.

- `shadowing.py` — every name here shadows a built-in
- `walrus_abuse.py` — an entire program's control flow expressed via `:=`
- `global_state_hell.py` — three functions, one global, zero coordination
