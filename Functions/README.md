# Functions

Crimes against purity, statelessness, and the reasonable expectation that calling a function twice with the same arguments does the same thing twice.

- `mutable_default_args.py` — the most famous Python footgun, weaponized
- `functions_returning_functions.py` — manual currying, six levels deep, and a nameless recursive lambda
