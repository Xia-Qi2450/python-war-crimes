# Classes

Crimes against the MRO, the Liskov substitution principle, and the assumption that a method's name at call time matches its name at definition time.

- `diamond_inheritance_hell.py` — four classes, one diamond, `super()` sorts it all out
- `metaclass_madness.py` — a metaclass that reverses every method name at class-creation time
